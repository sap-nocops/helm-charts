apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{include "presto-exporter.fullname" .}}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{include "presto-exporter.fullname" .}}
subjects:
  - kind: ServiceAccount
    name: {{ include "presto-exporter.serviceAccountName" .}}
    namespace: {{ .Release.Namespace }}
